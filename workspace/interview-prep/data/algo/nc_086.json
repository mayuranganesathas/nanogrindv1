{
  "id": "nc_086",
  "source": "neetcode_150",
  "blind75": true,
  "neetcode150": true,
  "leetcode_id": "207",
  "title": "Course Schedule",
  "url": "https://leetcode.com/problems/course-schedule/",
  "description": "There are a total of numCourses courses you have to take, labeled from 0 to numCourses - 1. You are given an array prerequisites where prerequisites[i] = [ai, bi] indicates that you must take course bi first if you want to take course ai. Return true if you can finish all courses, otherwise return false.",
  "constraints": [
    "1 <= numCourses <= 2000",
    "0 <= prerequisites.length <= 5000",
    "prerequisites[i].length == 2",
    "0 <= ai, bi < numCourses",
    "All the pairs prerequisites[i] are unique"
  ],
  "difficulty": "medium",
  "category": "Graphs",
  "pattern_tags": [
    "graphs",
    "topological_sort",
    "dfs"
  ],
  "estimated_minutes": 25,
  "phase": 1,
  "week": 5,
  "day": 5,
  "companies": [
    "amazon",
    "google",
    "meta"
  ],
  "optimal_time": "O(V+E)",
  "optimal_space": "O(V+E)",
  "brute_force_time": "O(V²+E)",
  "thinking_questions": [
    "'Finish course A before course B' creates a directed edge. When can't you finish all courses?",
    "What graph property prevents completing all courses?",
    "How do you detect a cycle in a directed graph?"
  ],
  "hints": {
    "L1": "Build a directed graph from prerequisites. You can complete all courses if and only if the graph has no cycle (it's a DAG).",
    "L2": "DFS cycle detection: track nodes in 'current path' (visiting) vs 'done'. If you hit a 'visiting' node, there's a cycle.",
    "L3": "# INSIGHT: detect cycle in directed graph using DFS with 3 states\n# 0=unvisited, 1=visiting(in current path), 2=done\nadj = defaultdict(list)\nfor a, b in prerequisites: adj[b].append(a)  # b must come before a\nstate = [0] * numCourses\ndef dfs(node):\n    if state[node] == 1: return False  # cycle!\n    if state[node] == 2: return True   # already processed\n    state[node] = 1  # mark visiting\n    for neighbor in adj[node]:\n        if not dfs(neighbor): return False\n    state[node] = 2  # mark done\n    return True\nreturn all(dfs(i) for i in range(numCourses))\n# Time: O(V+E)  Space: O(V+E)"
  },
  "test_cases": [
    {
      "input": "numCourses=2,prerequisites=[[1,0]]",
      "expected": "true"
    },
    {
      "input": "numCourses=2,prerequisites=[[1,0],[0,1]]",
      "expected": "false (cycle)"
    },
    {
      "input": "numCourses=1,prerequisites=[]",
      "expected": "true"
    },
    {
      "input": "numCourses=4,prerequisites=[[1,0],[2,1],[3,2]]",
      "expected": "true"
    },
    {
      "input": "numCourses=3,prerequisites=[[0,1],[1,2],[2,0]]",
      "expected": "false (3-cycle)"
    }
  ],
  "follow_up_questions": [
    "Why do you need three states (unvisited/visiting/done) rather than two (visited/unvisited)?",
    "How does Kahn's algorithm (BFS-based topological sort) solve this problem?",
    "What's the difference between cycle detection in undirected vs directed graphs?"
  ],
  "solution_notes": "DFS with 3 states (0=unseen, 1=visiting, 2=done). Cycle iff visit a 'visiting' node. O(V+E)."
}