{
  "id": "nc_056",
  "source": "neetcode_150",
  "blind75": true,
  "neetcode150": true,
  "leetcode_id": "98",
  "title": "Validate Binary Search Tree",
  "url": "https://leetcode.com/problems/validate-binary-search-tree/",
  "description": "Given the root of a binary tree, determine if it is a valid binary search tree (BST). A valid BST is defined as follows: the left subtree of a node contains only nodes with keys strictly less than the node's key; the right subtree of a node contains only nodes with keys strictly greater than the node's key; both the left and right subtrees must also be binary search trees.",
  "constraints": [
    "The number of nodes in the tree is in the range [1, 10^4]",
    "-2^31 <= Node.val <= 2^31 - 1"
  ],
  "difficulty": "medium",
  "category": "Trees",
  "pattern_tags": [
    "trees",
    "bst",
    "dfs"
  ],
  "estimated_minutes": 25,
  "phase": 1,
  "week": 4,
  "day": 1,
  "companies": [
    "amazon",
    "google",
    "microsoft"
  ],
  "optimal_time": "O(n)",
  "optimal_space": "O(h)",
  "brute_force_time": "O(n²)",
  "thinking_questions": [
    "Common mistake: only checking each node against its direct parent. Why doesn't that work? Give an example.",
    "What are the actual bounds on each node's value given its position in the tree?",
    "If you pass min and max bounds down the recursion, how do they update for left vs right children?"
  ],
  "hints": {
    "L1": "Checking only against parent isn't enough — a node in the right subtree must be greater than ALL ancestors, not just its parent. Pass valid bounds down the recursion.",
    "L2": "DFS with (min_bound, max_bound). Root: (-inf, +inf). Left child: (min, node.val). Right child: (node.val, max). Node valid if min < node.val < max.",
    "L3": "# INSIGHT: each node has global bounds derived from all ancestors\ndef isValidBST(root):\n    def valid(node, low, high):\n        if not node: return True\n        if not (low < node.val < high): return False\n        return valid(node.left, low, node.val) and valid(node.right, node.val, high)\n    return valid(root, float('-inf'), float('inf'))\n# Time: O(n)  Space: O(h)"
  },
  "test_cases": [
    {
      "input": "root=[2,1,3]",
      "expected": "true"
    },
    {
      "input": "root=[5,1,4,null,null,3,6]",
      "expected": "false (4<5)"
    },
    {
      "input": "root=[5,4,6,null,null,3,7]",
      "expected": "false (3<5 but in right subtree)"
    },
    {
      "input": "root=[1]",
      "expected": "true"
    },
    {
      "input": "root=[2,2,2]",
      "expected": "false (equal not allowed)"
    }
  ],
  "follow_up_questions": [
    "Give a concrete example where checking only against direct parent fails.",
    "Can you solve this with inorder traversal? What property of BST inorder traversal is useful?",
    "What if equal values were allowed on one side? How would you modify the bounds?"
  ],
  "solution_notes": "Pass (min, max) bounds down. Left updates max=node.val; right updates min=node.val. O(n), O(h). Alt: inorder traversal must be strictly increasing."
}