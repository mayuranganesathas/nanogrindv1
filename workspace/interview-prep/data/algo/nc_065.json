{
  "id": "nc_065",
  "source": "neetcode_150",
  "blind75": true,
  "neetcode150": true,
  "leetcode_id": "39",
  "title": "Combination Sum",
  "url": "https://leetcode.com/problems/combination-sum/",
  "description": "Given an array of distinct integers candidates and a target integer target, return a list of all unique combinations of candidates where the chosen numbers sum to target. You may return the combinations in any order. The same number may be chosen from candidates an unlimited number of times. Two combinations are unique if the frequency of at least one of the chosen numbers is different.",
  "constraints": [
    "1 <= candidates.length <= 30",
    "2 <= candidates[i] <= 40",
    "All elements of candidates are distinct",
    "1 <= target <= 40"
  ],
  "difficulty": "medium",
  "category": "Backtracking",
  "pattern_tags": [
    "backtracking",
    "arrays"
  ],
  "estimated_minutes": 25,
  "phase": 1,
  "week": 4,
  "day": 4,
  "companies": [
    "amazon",
    "google"
  ],
  "optimal_time": "O(n^(t/m))",
  "optimal_space": "O(t/m)",
  "brute_force_time": "O(n^(t/m))",
  "thinking_questions": [
    "Elements can be reused unlimited times. How does your 'start' index change vs Subsets?",
    "What's the pruning condition — when do you stop recursing?",
    "If the array is sorted, how can you prune branches early?"
  ],
  "hints": {
    "L1": "Unlike Subsets, you can reuse elements. When recursing after picking nums[i], pass i (not i+1) as the start.",
    "L2": "Sort first. Backtrack: if remaining==0 add combo. If remaining<0 return. For each i from start, recurse with remaining-nums[i] and start=i (allow reuse).",
    "L3": "# INSIGHT: allow reuse → recurse with same index i (not i+1)\ncandidates.sort()\nresult = []\ndef backtrack(start, current, remaining):\n    if remaining == 0: result.append(current[:]); return\n    for i in range(start, len(candidates)):\n        if candidates[i] > remaining: break  # sorted: pruning\n        current.append(candidates[i])\n        backtrack(i, current, remaining - candidates[i])  # i not i+1\n        current.pop()\nbacktrack(0, [], target)\nreturn result\n# Time: O(n^(t/m)) where t=target, m=min candidate\n# Space: O(t/m) recursion depth"
  },
  "test_cases": [
    {
      "input": "candidates=[2,3,6,7],target=7",
      "expected": "[[2,2,3],[7]]"
    },
    {
      "input": "candidates=[2,3,5],target=8",
      "expected": "[[2,2,2,2],[2,3,3],[3,5]]"
    },
    {
      "input": "candidates=[2],target=1",
      "expected": "[]"
    },
    {
      "input": "candidates=[1],target=1",
      "expected": "[[1]]"
    },
    {
      "input": "candidates=[1],target=2",
      "expected": "[[1,1]]"
    }
  ],
  "follow_up_questions": [
    "Why do we pass i (not i+1) when recursing to allow reuse?",
    "If elements couldn't be reused, what would change? (LC 40 — Combination Sum II)",
    "How does sorting enable the early break pruning?"
  ],
  "solution_notes": "Sort + backtrack. Pass same index i to allow reuse. break when candidate > remaining (sorted). O(n^(t/m))."
}