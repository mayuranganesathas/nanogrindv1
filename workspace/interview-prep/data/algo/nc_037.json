{
  "id": "nc_037",
  "source": "neetcode_150",
  "blind75": true,
  "neetcode150": true,
  "leetcode_id": "143",
  "title": "Reorder List",
  "url": "https://leetcode.com/problems/reorder-list/",
  "description": "You are given the head of a singly linked-list: L0 → L1 → ... → Ln-1 → Ln. Reorder it to: L0 → Ln → L1 → Ln-1 → L2 → Ln-2 → ... You may not modify the values in the list's nodes. Only nodes themselves may be changed.",
  "constraints": [
    "The number of nodes in the list is in the range [1, 5 * 10^4]",
    "1 <= Node.val <= 1000"
  ],
  "difficulty": "medium",
  "category": "Linked List",
  "pattern_tags": [
    "linked_list",
    "two_pointers"
  ],
  "estimated_minutes": 30,
  "phase": 1,
  "week": 3,
  "day": 1,
  "companies": [
    "amazon",
    "google"
  ],
  "optimal_time": "O(n)",
  "optimal_space": "O(1)",
  "brute_force_time": "O(n²)",
  "thinking_questions": [
    "The pattern is: first, last, second, second-to-last... The second half needs to be reversed and merged with the first. How do you split the list in half?",
    "Once you have the first half and reversed second half, how do you interleave them?",
    "What three operations are needed and in what order?"
  ],
  "hints": {
    "L1": "Break this into three steps: find the middle (slow/fast pointers), reverse the second half, then merge the two halves.",
    "L2": "1) Find mid with slow/fast pointers. 2) Reverse second half in-place. 3) Interleave: first→second_first→first_next→...",
    "L3": "# INSIGHT: find mid → reverse second half → interleave\n# Step 1: find middle\nslow, fast = head, head.next\nwhile fast and fast.next:\n    slow = slow.next\n    fast = fast.next.next\nsecond = slow.next\nslow.next = None  # split list\n# Step 2: reverse second half\nprev, curr = None, second\nwhile curr:\n    nxt = curr.next\n    curr.next = prev\n    prev, curr = curr, nxt\nsecond = prev\n# Step 3: interleave\nfirst = head\nwhile second:\n    tmp1, tmp2 = first.next, second.next\n    first.next = second\n    second.next = tmp1\n    first, second = tmp1, tmp2\n# Time: O(n)  Space: O(1)"
  },
  "test_cases": [
    {
      "input": "head=[1,2,3,4]",
      "expected": "[1,4,2,3]"
    },
    {
      "input": "head=[1,2,3,4,5]",
      "expected": "[1,5,2,4,3]"
    },
    {
      "input": "head=[1]",
      "expected": "[1]"
    },
    {
      "input": "head=[1,2]",
      "expected": "[1,2]"
    },
    {
      "input": "head=[1,2,3]",
      "expected": "[1,3,2]"
    }
  ],
  "follow_up_questions": [
    "Why does slow/fast pointer approach find the correct midpoint for even vs odd length lists?",
    "What goes wrong if you forget to set slow.next=None after splitting?",
    "How would the approach change for a doubly linked list?"
  ],
  "solution_notes": "Three steps: find mid (slow/fast), reverse second half, interleave. O(n), O(1)."
}