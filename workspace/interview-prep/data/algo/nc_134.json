{
  "id": "nc_134",
  "source": "neetcode_150",
  "blind75": false,
  "neetcode150": true,
  "leetcode_id": "1851",
  "title": "Minimum Interval to Include Each Query",
  "url": "https://leetcode.com/problems/minimum-interval-to-include-each-query/",
  "description": "You are given a 2D integer array intervals where intervals[i] = [lefti, righti] represents the ith interval starting at lefti and ending at righti (inclusive). You are also given an integer array queries. The answer to the jth query is the size of the smallest interval i such that lefti <= queries[j] <= righti. The size of an interval is defined as righti - lefti + 1. Return an array containing the answers to the queries. If no interval contains the query point, the answer is -1.",
  "constraints": [
    "1 <= intervals.length <= 10^5",
    "1 <= queries.length <= 10^5",
    "intervals[i].length == 2",
    "1 <= lefti <= righti <= 10^7",
    "1 <= queries[j] <= 10^7"
  ],
  "difficulty": "hard",
  "category": "Intervals",
  "pattern_tags": [
    "intervals",
    "heap",
    "offline-queries",
    "sweep"
  ],
  "estimated_minutes": 50,
  "phase": 2,
  "week": 6,
  "day": 3,
  "companies": [
    "google",
    "amazon"
  ],
  "optimal_time": "O((n + q) log n)",
  "optimal_space": "O(n + q)",
  "brute_force_time": "O(n * q)",
  "thinking_questions": [
    "Sort queries offline (with original indices). Sort intervals by start.",
    "As you sweep queries left to right, which intervals are 'active'?",
    "Use a min-heap of (size, end) for active intervals. How do you remove expired ones?"
  ],
  "hints": {
    "L1": "Sort queries offline. Use a min-heap of active intervals (by size). Add intervals as their start <= query, remove intervals whose end < query.",
    "L2": "Sort intervals by start, queries by value (with idx). Sweep: add intervals with start <= q to heap. Remove heap top while end < q. Answer = heap[0][0] or -1.",
    "L3": "# Minimum Interval to Include Query — O((n+q) log n)\n# KEY INSIGHT: offline queries + sweep + min-heap of active interval sizes\n\nimport heapq\n\ndef minInterval(intervals, queries):\n    intervals.sort()\n    sorted_queries = sorted(enumerate(queries), key=lambda x: x[1])\n    result = [-1] * len(queries)\n\n    heap = []  # (size, end)\n    i = 0      # pointer into sorted intervals\n\n    for idx, q in sorted_queries:\n        # Add all intervals starting at or before q\n        while i < len(intervals) and intervals[i][0] <= q:\n            start, end = intervals[i]\n            heapq.heappush(heap, (end - start + 1, end))\n            i += 1\n\n        # Remove intervals that have already ended\n        while heap and heap[0][1] < q:\n            heapq.heappop(heap)\n\n        if heap:\n            result[idx] = heap[0][0]  # smallest active interval size\n\n    return result"
  },
  "test_cases": [
    {
      "input": "intervals=[[1,4],[2,4],[3,6],[4,4]], queries=[2,3,4,5]",
      "expected": "[3,3,1,4]"
    },
    {
      "input": "intervals=[[2,3],[2,5],[1,8],[20,25]], queries=[2,19,5,22]",
      "expected": "[2,-1,4,6]"
    },
    {
      "input": "intervals=[[1,10]], queries=[5]",
      "expected": "[10]"
    }
  ],
  "follow_up_questions": [
    "Why sort queries offline instead of online?",
    "What does removing intervals with end < q guarantee?",
    "How would you handle duplicate queries efficiently?"
  ],
  "solution_notes": "Sort intervals by start, queries by value offline. Sweep: add intervals with start<=q to min-heap by size, pop expired ends. O((n+q)logn)."
}